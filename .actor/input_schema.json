{
  "title": "RozeePk Jobs Scraper",
  "description": "Scrape jobs from Rozee.pk. Provide either keyword/location/category OR a RozeePk search URL (startUrl, url, or startUrls). Actor paginates and optionally scrapes detail pages.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Start scraping from a specific RozeePk search URL. If provided, this will be used instead of Keyword/Location.",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Keyword (if not using Start URL)",
      "type": "string",
      "description": "Job search keyword(s), e.g., 'Software Developer', 'Marketing Manager', 'Data Analyst'.",
      "editor": "textfield",
      "prefill": "software engineer"
    },
    "location": {
      "title": "Location (if not using Start URL)",
      "type": "string",
      "description": "Location filter for jobs.",
      "editor": "textfield",
      "prefill": "Lahore"
    },
    "collectDetails": {
      "title": "Collect job details",
      "type": "boolean",
      "description": "If enabled, scraper opens job detail pages for full description.",
      "editor": "checkbox",
      "default": true
    },
    "results_wanted": {
      "title": "Maximum number of jobs",
      "type": "integer",
      "description": "The maximum number of jobs to collect. Leave empty to collect all available jobs.",
      "minimum": 1,
      "default": 100,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum number of pages",
      "type": "integer",
      "description": "A safety cap on the number of search result pages to visit.",
      "minimum": 1,
      "editor": "number",
      "default": 20
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Use Apify Proxy (datacenter recommended for RozeePk).",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    },
    "dedupe": {
      "title": "Deduplicate results",
      "type": "boolean",
      "description": "Remove duplicate job URLs from results (in-memory deduplication).",
      "editor": "checkbox",
      "default": true
    }
  },
  "required": [],
  "additionalProperties": true
}